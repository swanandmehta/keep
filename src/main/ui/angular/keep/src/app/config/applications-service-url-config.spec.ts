import { ApplicationsServiceUrlConfig } from './applications-service-url-config';

describe('ApplicationsServiceUrlConfig', () => {
  it('should create an instance', () => {
    expect(new ApplicationsServiceUrlConfig()).toBeTruthy();
  });
});
