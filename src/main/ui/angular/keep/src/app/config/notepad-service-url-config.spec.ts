import { NotepadServiceUrlConfig } from './notepad-service-url-config';

describe('NotepadServiceUrlConfig', () => {
  it('should create an instance', () => {
    expect(new NotepadServiceUrlConfig()).toBeTruthy();
  });
});
