import { NgbModalOptions } from '@ng-bootstrap/ng-bootstrap';

export class ModelConfig {

    public static readonly newNoteModelOptions: NgbModalOptions = {
        keyboard: false,
        scrollable: true,
    }
}
