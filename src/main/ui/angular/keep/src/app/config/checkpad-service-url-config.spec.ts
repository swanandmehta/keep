import { CheckpadServiceUrlConfig } from './checkpad-service-url-config';

describe('CheckpadServiceUrlConfig', () => {
  it('should create an instance', () => {
    expect(new CheckpadServiceUrlConfig()).toBeTruthy();
  });
});
