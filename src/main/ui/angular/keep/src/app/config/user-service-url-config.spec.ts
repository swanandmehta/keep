import { UserServiceUrlConfig } from './user-service-url-config';

describe('UserServiceUrlConfig', () => {
  it('should create an instance', () => {
    expect(new UserServiceUrlConfig()).toBeTruthy();
  });
});
